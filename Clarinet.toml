[project]
name = "reserve-authenticator"
authors = ["Your Name"]
description = "A robust blockchain-native access control and authentication system"
telemetry = false
requirements = []
[contracts.reserve-authenticator]
path = "contracts/reserve-authenticator.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
